#include <iostream>
#include <vector>

int main() {
    std::vector<int> vec;
    int n;
    while (true) {
        // vec.push_back(0);
        n = vec.size();
    }
    std::cout << n << std::endl;
    return 0;
}